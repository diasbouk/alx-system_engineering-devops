#!/usr/bin/env bash
# File info
file="school"
if [[ -z $file ]]; then
	echo "file school does not exist"
fi
if [[ -n $file ]]; then
	echo "file school exists"
	if [[ ! -s $file ]]; then
		echo "school file is empty"
	else
		echo "school file is not empty"
	fi
	if [[ -f $file ]]; then
		echo "school is a regular file"
	fi
fi
